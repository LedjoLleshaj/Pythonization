# inpython everythig is an object
# so also modules are objects
# we can see the attributes of a module

import calculator #i can import  only files in the same directory
# if a file is in a different directory i have to specify the path


print(calculator.creator)
print(calculator.PI)
print(calculator.add(1, 2))
print(calculator.subtract(1, 2))
print(calculator.multiply(1, 2))
print(calculator.divide(1, 2))
print(calculator.area_of_circle(1))
